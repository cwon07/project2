<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head.ejs') %>
<body>
    <style>
        form {
                margin: 80px;
            }
        legend {
            font-family: 'Solway', serif;
            font-size: 2.5rem;
            text-align: center;
            margin: 20px;
        }
        .bottom-links {
            text-align: center;
        }
        </style>
    <%- include('../partials/header.ejs') %>
    <main>
        <div>
            <form method="POST" action="/recipes/<%=recipe._id%>?_method=PUT">
                <fieldset>
                    <legend>Edit <%= recipe.title %></legend>
               
                <label>Title: <input type="text" name="title" value="<%=recipe.title%>"/></label>
                <label>Type: <input type="text" name="type" value="<%= recipe.type%>"/></label>
                <label>Image: <input type="text" name="image" value="<%= recipe.image%>"/></label>
                <label> Ingredients: 
                    <textarea id="textarea" input type="text" name="ingredients" value="<%= recipe.ingredients%>"></textarea></label>
                <label> Instruction: 
                    <textarea id="textarea" input type="text" name="instruction" value="<%= recipe.instruction%>"></textarea></label>
                </label>
                <div class="bottom-links">
                <button style="background-color: transparent; color: darkslategray; border-color: transparent; font-size: 1.4rem">EDIT RECIPE</button>
                <a class="nav-link fw-bold py-1 px-0" href="/recipes"><button style="background-color: transparent; color: darkslategray; border-color: transparent; font-size: 1.4rem">BACK TO LIST</a></button>
            </div>
            </fieldset>
            </form>
        </div>
    </main>
</body>
</html>